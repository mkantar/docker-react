language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t muharremkantar/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true muharremkantar/docker-react npm run test